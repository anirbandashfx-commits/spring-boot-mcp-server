# VS Code Cursor Setup

How to connect VS Code Cursor to the MCP Studio server.

1. Start the MCP Studio server:
   ```bash
   mvn spring-boot:run
   ```
2. Use the SSE server URL `http://localhost:8081/sse` in VS Code Cursor's configuration.
